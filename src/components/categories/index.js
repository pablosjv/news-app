import React from 'react';
import App from '../App/index';

export const Python = () => <App defaultQuery="python" disbleSearch={true} />;
export const Javascript = () => (
  <App defaultQuery="javascript" disbleSearch={true} />
);
